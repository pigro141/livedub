# Le dipendenze di livedub. Non esistevano: il venv e' gitignorato, quindi da un
# clone pulito l'ambiente non era ricostruibile e le scelte fatte qui sotto erano
# invisibili a chi leggeva il repo.
#
#   .\.venv\Scripts\python.exe -m pip install -r requirements.txt
#   .\.venv\Scripts\python.exe -m pip install -r requirements-nodeps.txt --no-deps
#
# **Sono due comandi e non uno, e il secondo non e' un optional**: senza, il
# lettore e la voce non ci sono affatto. I quattro pacchetti che stanno di la'
# dipendono da `onnxruntime` (CPU), che accanto a `onnxruntime-gpu` spegne la CUDA
# in silenzio; `--no-deps` e' l'unico modo di tenerlo fuori, ed e' un'opzione
# **globale** — messa qui spegnerebbe le dipendenze di tutto il resto. E' la
# stessa ragione per cui argostranslate sta in uno script e non qui: un
# `requirements.txt` non sa dire «questo pacchetto senza le sue dipendenze».
# `installa.ps1` fa la sequenza giusta e poi **verifica di averla ottenuta**.
#
# Le versioni sono quelle verificate sulla macchina di sviluppo (Windows 11,
# Python 3.11.9, i9-11900K + RTX 4060), non minimi teorici.

# --- runtime ONNX: la riga che non e' intercambiabile ------------------------
# **`onnxruntime-gpu` e NON `onnxruntime`**, ed e' una scelta di architettura, non
# un dettaglio di installazione. Kokoro su CPU costa 725 ms a battuta contro i 207
# su CUDA: senza GPU quel motore non e' vivibile. E' un superset — ECAPA
# (`listen/embed.py`) e rapidocr continuano a girare su CPU esattamente come
# prima, verificato con la suite verde e con il passo di Piper invariato — ma i
# due pacchetti **non convivono**: installare `onnxruntime` accanto a questo
# rompe la risoluzione delle DLL. Se si toglie la GPU, si torna a `onnxruntime` e
# si mette `tts.backend=piper`.
#
# Gli extra [cuda,cudnn] tirano i pacchetti pip nvidia-*: senza, ORT non trova le
# DLL e **ripiega sulla CPU in silenzio**, che e' il modo in cui 708 ms stavano
# per essere riportati come "il numero della GPU".
onnxruntime-gpu[cuda,cudnn]==1.28.0

# --- sintesi -----------------------------------------------------------------
# **`piper-tts` va installato con `--no-deps`**, e non e' una rifinitura: tira
# `onnxruntime` CPU, che accanto a `onnxruntime-gpu` **spegne la CUDA in
# silenzio** (misurato: restano `AzureExecutionProvider, CPUExecutionProvider`).
# E' la stessa trappola gia' scritta piu' sotto per `minisbd`, su un pacchetto
# diverso.
#
# La 1.6.0 e' bloccata da Smart App Control: `espeakbridge.pyd` non carica — e
# non si vede all'installazione, perche' quel `.pyd` si apre **pigramente alla
# prima sintesi**. Cioe' il difetto compare a sessione accesa, sulla prima
# battuta, che e' il momento peggiore.
# **I tre motori stanno in `requirements-nodeps.txt`**, e il perche' e' misurato.
# L'avviso qui sopra riguardava il solo Piper, e i pacchetti che tirano dentro
# `onnxruntime` (CPU) sono **quattro** — i tre motori piu' il lettore di serie,
# `rapidocr-onnxruntime`, che nessun commento nominava. La tabella e la misura
# stanno in quel file.
#
# Le loro dipendenze vere — quelle che non sono `onnxruntime` — sono elencate a
# mano qui sotto, perche' con `--no-deps` non le porta piu' nessuno.
espeakng-loader==0.2.4      # kokoro: i binari espeak-ng per il g2p
phonemizer-fork==3.3.2      # kokoro: il g2p italiano
# `tokenizers` stava qui per il backend Qwen, che e' stato tolto: adesso non lo
# importa piu' nessuno (`llama-cpp-python` porta il suo). Tolto anche da qui —
# una dipendenza dichiarata e non usata fa reinstallare a ogni clone qualcosa che
# non serve, e la prossima volta che la si legge nessuno sa piu' perche' c'era.

# --- traduzione e correzione del testo (opzionali) ---------------------------
# **Un solo modello locale per due lavori**: tradurre i sottotitoli e scegliere
# la parola giusta fra i candidati quando l'OCR ha letto male. Gira su **CPU**,
# di proposito: la GPU serve alla sintesi, e la prova hardware di Qwen ha
# misurato quanto poco margine ci sia.
#
# **Non e' piu' una riga di questo file, e questa e' la cura di un difetto che
# fermava tutta l'installazione.** `llama-cpp-python==0.3.34` era un requisito
# normale, e su Windows **non esiste in nessuno dei due posti da cui questo stesso
# file dice di prenderlo**. Misurato il 25 agosto:
#
#  - su PyPI la 0.3.34 ha **un solo file, l'sdist**: niente ruota, quindi pip
#    compila. Riprodotto con `pip wheel --no-deps --no-cache-dir`: `CMake Error:
#    CMAKE_C_COMPILER not set`. Su una macchina senza MSVC — cioe' su una macchina
#    normale — muore li';
#  - sull'indice CPU di abetlen indicato qui sotto, l'ultima ruota `cp311-
#    win_amd64` e' la **0.3.19**: la 0.3.34 non c'e' nemmeno li'.
#
# E siccome era la **prima riga che pip incontra dopo la risoluzione**, il comando
# in testata a questo file non installava *niente*: chi clonava il repo restava a
# mani vuote. E' la forma peggiore fra quelle gia' viste, perche' il file
# contraddiceva se stesso — la riga installabile diceva una cosa e il commento
# sopra ne diceva un'altra.
#
# Resta qui come **facoltativo**, ed e' onesto che lo sia: `translate.backend`
# vale `locale` di serie, non `llm`. Chi lo vuole, con la ruota che esiste:
#
#   .\.venv\Scripts\python.exe -m pip install llama-cpp-python==0.3.19 `
#       --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu `
#       --only-binary :all:
#
# Il modello (`gemma-3-1b-it-Q4_K_M.gguf`, ~800 MB) si scarica da solo in
# `models/llm/` alla prima chiamata.
#
# **Con Smart App Control acceso non carica, e non ce n'e' una versione che lo
# faccia.** Provate 0.3.35 / 0.3.34 / 0.3.2 da PyPI e la ruota dell'indice CPU
# di abetlen qui sopra, che e' un **file diverso** e quindi meritava una prova a
# se': bloccata pure quella. Qui il pezzo nativo non e' nemmeno un `.pyd` — e'
# `llama_cpp/lib/llama.dll`, aperta con `ctypes` — il che dice che il criterio
# non guarda come si carica una libreria, guarda **quale**.
#
# Non e' un `ImportError`, e la guardia scritta per prenderlo non scattava: quel
# che esce e' `RuntimeError: Failed to load shared library ... [WinError 4551]`.
# Adesso passa da `core/bloccati.py`, che lo dichiara e manda su «locale» o
# «ollama» invece di consegnare quella riga — ed e' lo stesso modulo che dice
# «assente» quando non lo si e' installato affatto, che e' il caso di serie.

# --- la traduzione offline, e le sue due trappole ----------------------------
# **Non la installa nessuno dei due comandi in testata, e non e' una
# dimenticanza.** E' il default di `translate.backend`, ma costa ~3 GB — `stanza`
# tira `torch`, che la traduzione non usa mai — e su GTA V in italiano non serve:
# `translate.enabled` e' false di serie. Farli pagare a chiunque installi il
# programma, per una cosa che quasi nessuno accende, e' il contrario di una
# scelta.
#
# **Arriva quando serve, e dicendo prima quanto pesa.** Chi accende la traduzione
# lo scopre al passo 6 della guida (`core/banco.py`), che e' lo stesso meccanismo
# gia' usato per i 541 MB dei modelli: guarda cosa manca, **dichiara la
# dimensione prima** che uno decida, e consegna la riga da incollare. La consegna
# invece di eseguirla perche' questi sono **pacchetti** e il banco non fa `pip`:
# un `pip install argostranslate` ingenuo tira dentro `onnxruntime` (CPU) e
# spegne la CUDA in silenzio. Se non arriva, e' una **rinuncia dichiarata**
# (`core.banco.AVVISI`), non un ripiego muto.
#
# La **coppia di lingue** invece e' un modello, pesa 98 MB, e quella il banco la
# scarica da solo — ma solo con la traduzione accesa.
#
# Le righe qui sotto sembrano barocche e non lo sono. `pip install argostranslate`
# e basta fa **due** danni, misurati con `--dry-run` prima di installare:
#
#  1. tira `minisbd`, che dipende da `onnxruntime` (CPU). Quel pacchetto e
#     `onnxruntime-gpu` scrivono nella stessa cartella: installarlo spegne la
#     CUDA di Kokoro **in silenzio** — 725 ms a battuta invece di 207, con i log
#     verdi. Per questo `minisbd` va messo con `--no-deps`: `onnxruntime-gpu` e'
#     gia' un superset e fornisce lo stesso modulo.
#  2. tira `spacy` (~100 MB) che ad argos serve solo se si sceglie quel modo di
#     spezzare le frasi: il default e' `ARGOSTRANSLATE`, e `sbd.py` importa spacy
#     dentro un `try`. Con `--no-deps` sull'argostranslate e le dipendenze vere
#     elencate a mano, resta fuori.
#
# Quello che **non** si puo' evitare: `argostranslate/sbd.py` importa `stanza` in
# cima al modulo, e stanza dipende da `torch`. Sono +712 MB di venv (misurati:
# 3120 -> 3832 MB) per una traduzione che gira su CTranslate2 e non usa torch per
# niente. E' il prezzo della traduzione offline, ed e' scritto qui perche' chi
# ricostruisce l'ambiente lo sappia prima e non dopo.
#
# **E per questo non stanno in questo file.** Un `requirements.txt` non sa dire
# «questo pacchetto senza le sue dipendenze»: `--no-deps` esiste solo come
# opzione globale, e globale spegnerebbe le dipendenze di *tutto* il resto.
# Scriverli qui come righe normali vorrebbe dire che il prossimo `pip install -r`
# reinstalla `onnxruntime` CPU e spegne la GPU — cioe' il file che serve a
# rendere l'ambiente ricostruibile lo romperebbe. Stanno in uno script, che fa
# la sequenza giusta ed e' rieseguibile:
#
#   powershell -ExecutionPolicy Bypass -File tools\installa_traduzione.ps1
#
# Le versioni installate e verificate: argostranslate 1.11.0, minisbd 0.9.5,
# ctranslate2 4.8.1, sentencepiece 0.2.2, sacremoses 0.1.1, stanza 1.10.1
# (+ torch 2.13.0, che si porta dietro stanza).

# --- visione e riconoscimento ------------------------------------------------
#
# **Le versioni qui sotto non sono "l'ultima che funziona": sono le uniche che
# Smart App Control lascia caricare**, ed e' un vincolo del prodotto e non di una
# macchina. SAC e' **acceso di serie sulle installazioni pulite di Windows 11**,
# quindi cio' che blocca lo blocca a chiunque scarichi il programma.
#
# Il criterio non e' «non firmato» — numpy e ctranslate2 non lo sono e passano —
# ed e' la **reputazione del singolo file** (ISG): non «vecchio passa, nuovo no»,
# tanto che opencv 4.14 e' bloccata e la 4.13 no. Quindi ogni riga qui sotto e'
# stata provata **importandola in un venv nuovo**, non dedotta.
#
# ⚠️ E non si verifica dentro un `.venv` che ha quei file da mesi: misurato,
# `ctranslate2/_ext.pyd` carica qui dentro mentre la sua copia **byte-identica**
# (stesso sha256) e' bloccata. Il verdetto sta in cache **sulla copia**, non sul
# contenuto — quindi un ambiente gia' installato **non puo' esprimere** la
# risposta alla domanda «cosa succede a chi installa adesso», e ogni verde letto
# li' e' falso. E' la stessa forma gia' costata cinque volte in questo progetto:
# una misura che non puo' dare la risposta che le si chiede.
# `rapidocr-onnxruntime` sta in `requirements-nodeps.txt`: chiede
# `onnxruntime>=1.7.0`, cioe' il lettore **di serie** da solo rimetteva dentro la
# ruota CPU. Le sue dipendenze vere sono queste — le quattro qui sotto sono nuove
# in questo file solo perche' prima arrivavano da lui.
six==1.17.0                 # rapidocr
shapely==2.1.2              # rapidocr
PyYAML==6.0.3               # rapidocr
tqdm==4.70.0                # rapidocr, e la barra di huggingface_hub
opencv-python==4.13.0.92    # 5.0.0.93 e 4.14 bloccate; provate morphologyEx,
                            # find_bands e inchiostro(), non solo l'import
pillow==11.3.0              # era 12.3.0 (codec opzionali bloccati)
pyclipper==1.3.0.post6      # rapidocr tira la 1.4.0, bloccata
cffi==2.0.0                 # soundfile tira la 2.1.0, bloccata
rpds-py==0.27.1             # jsonschema (via kokoro) tira la 2026.6.3, bloccata
mss==10.2.0
# `oneocr` sono **due cose**, e questo commento per mesi ne ha dichiarata una
# sola: le DLL si copiano da Windows con `tools/fetch_oneocr.py` e girano in un
# processo figlio perche' portano la **propria** onnxruntime.dll e pretendono
# un'altra versione di API — ma il figlio fa anche `import oneocr`, che e' un
# pacchetto pip, e sta in `requirements-nodeps.txt`.
# La riga di prima diceva «non e' un pacchetto pip» punto e basta, e ha fatto da
# lucchetto: chi si e' chiesto se mancasse ha letto qui che non doveva esserci.
# Un commento che nega l'esistenza di una dipendenza e' piu' pericoloso di una
# dipendenza dimenticata, perche' chiude la domanda invece di lasciarla aperta.

# --- audio -------------------------------------------------------------------
soundfile==0.14.0
# **PyAudioWPatch e non `sounddevice`**: la cattura serve il *loopback* WASAPI —
# sentire quello che il gioco sta suonando — e quello e' precisamente cio' che il
# patch aggiunge a PyAudio. E' anche il motivo per cui questo progetto e' legato a
# Windows (`capture/audio.py`).
PyAudioWPatch==0.2.12.8
numpy==2.4.6

# --- la finestra, che e' il prodotto ----------------------------------------
# **Mancava**, e non e' un dettaglio: `tools/ui_qt.py` e' cio' che apre
# `livedub.bat`, cio' che impacchetta `livedub.spec` e cio' che l'utente chiama
# «il programma». Da un clone pulito, `installa.ps1` installava tutta la catena
# — sintesi, visione, audio, traduzione — e poi il doppio clic moriva su
# `ModuleNotFoundError: PySide6`. Il difetto era invisibile qui dentro perche'
# nessun altro pacchetto la tira: su questa macchina c'era e basta.
#
# La Tk (`tools/ui.py`) non e' un ripiego: non e' vestita Menta e non lo sara'.
PySide6==6.11.1

# --- modelli e video ---------------------------------------------------------
huggingface_hub==1.25.1     # scarica Piper, ECAPA e Kokoro dentro models/
imageio-ffmpeg==0.6.0       # il montaggio degli MP4 di prova (tools/dub.py --mp4)

# Windows Graphics Capture: la cattura di **una finestra sola**.
# Non e' una comodita': catturando lo schermo, nel fotogramma che va
# all'OCR finisce anche cio' che sta davanti al gioco — comprese le
# nostre finestre — e il programma legge se stesso.
#
# **Su una macchina con Smart App Control acceso non carica, e non ce n'e' una
# versione che lo faccia.** Provate 2.0.1 / 2.0.0 / 1.5.0 / 1.4.4, e provato
# anche l'unico altro pacchetto che espone la stessa API — `winrt-Windows.
# Graphics.Capture` di pywinrt, in tutte e cinque le versioni pubblicate
# (3.2.1 / 3.1.0 / 3.0.0 / 2.3.0 / 2.0.1) — che porta giu' anche il suo
# `winrt-runtime`. Non e' quella libreria: e' **ogni file nuovo**, perche' il
# criterio guarda la reputazione della singola copia.
#
# Resta qui perche' dove SAC e' spento e' il modo migliore, e perche' il ripiego
# e' **dichiarato**: senza, `capture/screen.py` passa a `PrintWindow` di
# `user32.dll` scrivendolo nel registro (`capture.screen.apri_finestra`).
# `PrintWindow` cattura la stessa finestra senza niente da installare — 6,2 ms
# per la fascia che si legge contro 17,6 per la finestra intera — e ha un
# difetto che WGC non ha: su un gioco Direct3D puo' **riuscire e restituire
# nero**. Anche quello e' dichiarato (`PrintWindowSource.nero`).
windows-capture
